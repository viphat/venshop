<% unless f.empty? %>
  <div class="alert alert-<%= f.first.in?(['notice','success']) ? 'success' : 'danger' %>">
    <a href="#" data-dismiss="alert" class="close">×</a>
    <ul>
      <li><%= f.second %></li>
    </ul>
  </div>
<% end %>
